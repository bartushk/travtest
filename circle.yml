dependencies:
  pre:
    - sudo apt-get update
    - apt-cache search icd
    - sudo apt-get install -y cmake nvidia-opencl-icd-340 xz-utils
  override:     
    - wget https://cmocka.org/files/1.0/cmocka-1.0.1.tar.xz
    - tar xf cmocka-1.0.1.tar.xz
    - cd cmocka-1.0.1 && mkdir build && cd build && cmake -DCMAKE_INSTALL_PREFIX=/usr -DCMAKE_BUILD_TYPE=Release .. && make && sud
      
test:
    override:
    - make
